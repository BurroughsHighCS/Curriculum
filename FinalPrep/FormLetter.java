public class FormLetter {
	public String replaceAll(String line, String sub, String repl) {
		while(1)
		{
			//search line for index of sub
			//delete indexes for the lengh of sub
			//add repl into that index
			//break once no more indexes of sub are found
		}
		return line;
	}
}